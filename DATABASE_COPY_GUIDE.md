# دليل نسخ ومشاركة قاعدة البيانات SQLite

## 📋 نظرة عامة

قاعدة البيانات الحالية تستخدم **SQLite** وتحتوي على جميع البيانات والهيكل التنظيمي.

## 📁 موقع ملف قاعدة البيانات

ملف قاعدة البيانات موجود في:
```
database/database.sqlite
```
أو في الجذر:
```
database.sqlite
```

## 🔄 خطوات نسخ قاعدة البيانات

### 1. نسخ الملف

#### على Windows:
```powershell
# نسخ ملف قاعدة البيانات
Copy-Item database.sqlite database_backup.sqlite
```

#### على Linux/Mac:
```bash
# نسخ ملف قاعدة البيانات
cp database.sqlite database_backup.sqlite
```

### 2. إرسال الملف

يمكنك إرسال ملف `database.sqlite` عبر:
- البريد الإلكتروني (إذا كان الحجم صغير)
- Google Drive / Dropbox / OneDrive
- GitHub (إذا كان المشروع خاص)
- USB Drive
- أي وسيلة مشاركة ملفات أخرى

## 📥 خطوات استلام قاعدة البيانات (للشخص الآخر)

### الخطوة 1: نسخ ملف قاعدة البيانات

ضع ملف `database.sqlite` في مجلد المشروع في أحد المواقع التالية:

**الخيار الأول (موصى به):**
```
project-root/database/database.sqlite
```

**الخيار الثاني:**
```
project-root/database.sqlite
```

### الخطوة 2: التأكد من إعدادات قاعدة البيانات

تأكد من أن ملف `.env` يحتوي على الإعدادات الصحيحة:

```env
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite
```

أو إذا وضعت الملف في الجذر:
```env
DB_CONNECTION=sqlite
DB_DATABASE=database.sqlite
```

### الخطوة 3: التأكد من الصلاحيات (على Linux/Mac)

إذا كنت تستخدم Linux أو Mac، تأكد من أن الملف لديه صلاحيات القراءة/الكتابة:

```bash
chmod 644 database/database.sqlite
```

### الخطوة 4: اختبار الاتصال

قم بتشغيل الأمر التالي للتحقق من اتصال قاعدة البيانات:

```bash
php artisan migrate:status
```

أو:

```bash
php artisan db:show
```

## ⚠️ ملاحظات مهمة

### 1. لا تحتاج لتشغيل Migrations
إذا كنت تشارك ملف قاعدة البيانات الكامل، **لا تحتاج** لتشغيل `php artisan migrate` لأن جميع الجداول والبيانات موجودة بالفعل.

### 2. تأكد من التوافق
- تأكد من أن الشخص الآخر يستخدم نفس إصدار Laravel
- تأكد من أن جميع الـ Migrations موجودة في المشروع

### 3. نسخ احتياطي
**قبل استبدال قاعدة البيانات، يجب على الشخص الآخر عمل نسخة احتياطية من قاعدة البيانات الحالية** (إن وجدت):

```bash
# نسخ احتياطي
cp database/database.sqlite database/database_backup_$(date +%Y%m%d).sqlite
```

### 4. تنظيف Cache
بعد استبدال قاعدة البيانات، قد تحتاج إلى تنظيف الكاش:

```bash
php artisan config:clear
php artisan cache:clear
```

## 🔍 التحقق من نجاح العملية

يمكن للشخص الآخر التحقق من نجاح استيراد قاعدة البيانات عن طريق:

### 1. التحقق من عدد السجلات:
```bash
php artisan tinker
>>> DB::table('users')->count()
>>> DB::table('organizations')->count()
>>> DB::table('branches')->count()
```

### 2. التحقق من البيانات:
```bash
php artisan tinker
>>> DB::table('users')->first()
```

## 📦 بديل: تصدير البيانات فقط (بدون الهيكل)

إذا أردت مشاركة البيانات فقط وليس الهيكل الكامل، يمكنك تصدير البيانات:

### تصدير SQL Dump:
```bash
sqlite3 database.sqlite .dump > database_export.sql
```

ثم يمكن للشخص الآخر استيرادها:
```bash
sqlite3 database.sqlite < database_export.sql
```

## 🎯 ملخص سريع

**للمرسل:**
1. نسخ ملف `database/database.sqlite`
2. إرساله للشخص الآخر

**للمستقبل:**
1. نسخ احتياطي من قاعدة البيانات الحالية (إن وجدت)
2. وضع ملف `database.sqlite` في `database/` أو الجذر
3. التأكد من إعدادات `.env`
4. اختبار الاتصال
5. تنظيف الكاش

## 📝 مثال عملي

```bash
# على جهازك (المرسل)
cd C:\development\Hudayi-Laravel-new-release
Copy-Item database.sqlite database_backup.sqlite
# ثم أرسل database.sqlite

# على جهاز الشخص الآخر (المستقبل)
cd /path/to/project
# نسخ احتياطي
cp database/database.sqlite database/database_backup_original.sqlite
# نسخ الملف الجديد
cp /path/to/received/database.sqlite database/database.sqlite
# التحقق
php artisan db:show
php artisan config:clear
```

